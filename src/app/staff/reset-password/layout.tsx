export const dynamic = "force-dynamic";

export const metadata = {
  title: "Reset password — Evolve Staff",
  robots: { index: false, follow: false, nocache: true },
};

export default function StaffResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
